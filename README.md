`problem.js` - is a simple race condition example with i/o operations.
`solution.js` - is a solution to avoid race conditions.
If you are working with powerfull database like Postgres, Redis or Mongo, you can not meet with race conditions, because db protect data by itself. 